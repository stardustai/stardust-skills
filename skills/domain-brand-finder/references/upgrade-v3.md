# v3 upgrade verification — 2026-09-14

Scope: installed skill name unchanged; generic naming methods and evidence gates are separate from project briefs. Historical research and scores are not rewritten.

Implemented: four entry modes; seven construction operations separated from semantic sources and phonetic attributes; names-first independent review; frozen v3 weights; domain acquisition and US preliminary/public-use evidence as separate gates; honest partial delivery and no automatic high scores.

Domain tools now normalize complete-domain cache identities, expire evidence, reject unsupported/error/invalid controls, preserve current candidate metadata and raw receipts, and distinguish registry status from a purchase path. Output paths cannot overwrite input/cache/history or one another. Legacy CSV is a compatibility view, not clearance evidence.

The US read-only helper preserves official records and raw responses, validates positive controls and complete result counts, and returns unknown for unsupported query shapes. Three-or-more-word and multiword fuzzy requests currently need manual verification. No script issues PASS_SCREEN automatically. The delivery validator checks supplied structured records, receipts, chronology, independent reviewer identity, quote amounts/terms, and route counts; semantic and legal judgments remain the principal reviewer's responsibility.

Verification at this implementation checkpoint:

- 106 automated tests passed under Python 3 / pytest, including intentionally failing regressions before each repair.
- Skill frontmatter/structure validation passed; whitespace diff check passed.
- Three fixed old/new behavior pairs cover Friday with incomplete evidence, a non-AI creative-only brief, and domains-only with stale/failed evidence. Both versions already resisted some obvious false claims; the observed improvements are method labeling, scope separation and explicit evidence boundaries, not proof of superior commercial outcomes.
- Six real registered domain controls passed across ai/com/io/dev/chat/tech; the US EVOLIA single-word and STAR TREK two-word controls returned official records. These are tool reachability/shape checks, not candidate clearance.

Reusable test invocation:

```bash
python3 -m pytest /absolute/path/to/domain-brand-finder/tests -q
```

Project-specific source documents, raw evaluation responses, control receipts, actual candidates, prices and reviewer decisions belong in that project's run directory. They are deliberately not embedded in this reusable skill. The Friday 20-candidate run is a separate acceptance item and was not complete at this checkpoint.

Follow-up live-run repair: a424-result positive control exposed the single400-record page limit. Sequential pagination now preserves every page, exact totals, unique serials and required fields, with25-page/10,000-result caps. Regression baseline:4 failed/1 passed before repair; the complete suite is now113 passed, independently rerun by the principal. Live control independently verified424 unique records in pages400+24 and its known registration. Actual duplicate-page records and null literal-wordmark fields remained INCOMPLETE; they were not promoted to clearance. Whole-word borrowings mislabeled as new derivations/compounds now have explicit correction guidance.

Later same-day checkpoint: pagination now retains valid later pages when one source record has a metadata gap, and treats empty owner/history and goods/services arrays as incomplete. The full principal-rerun suite is119 passed plus18 subtests. Two observed real-run behavior gaps prompted explicit guidance on unscored-triage calibration and full native-catalog/developer receipts. Two additional supervised fixture-only Luna responses passed principal review: they refused quota-driven score promotion and unsupported PASS_SCREEN from missing/truncated receipts. These small behavior checks are not a blind benchmark. Exact public control identities must be verified from current sources; remembered repository owners are not authoritative. Frontmatter validation and scoped whitespace checks passed. Actual project receipts and principal readback remain in the separate project directory.

## Creative framework extension — 2026-09-14

The scoring rubric remains v3; this is a creative-workflow extension, not a score conversion or weakened evidence gate. The historical twelve directions now map to a selectable catalog of nine source/borrowing templates and fourteen construction templates, with sound/form goals and perceptual diversity reviewed separately. Vocabulary pools and special-source/coinage exceptions are explicit project choices; Oxford 5000 is only an optional English example. Agreed pattern rows default to five distinct creative candidates, not five automatically qualified finalists or a mandatory all-catalog run.

Creative-first requests now stop for user alignment before public-commercial-use, domain and trademark queries. A separate creative-round asset preserves row counts, provenance, first impressions, feedback and gaps. Formal `route` counts remain perceptual routes; `primary_creative_row`/`creative_plan_ref` preserve creative provenance without pretending the current validator checks it. Existing generator self-scores and historical reviewer scores are retained, not adopted as current passes or erased from history.

Validation: existing tool suite freshly passed 144 tests and 18 subtests. Added reusable behavior scenarios 6–10 cover integrated planning, duplicate/count pressure, pool and surface-form boundaries, cross-project scope and score provenance. Independent Luna responses and principal review exposed an old operation-only bias, confirmed the updated boundary behavior, and prompted explicit clarification of historical self-score preservation. Independent document review prompted the separate creative-row metadata above. These are bounded qualitative checks, not statistical evidence of improved names. No candidates, domain quotes, trademark searches, purchases or public launches were produced by this extension.
