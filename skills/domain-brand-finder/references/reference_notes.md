# Historical Reference Notes

This file preserves setup provenance, not current instructions. Follow SKILL.md and the routed methods/review/screening references. The former AI-only preferences and CSV-only evidence model below have been superseded by configurable project briefs and separate evidence tables.

Closest external reference found during setup:

- `softaworks/agent-toolkit@domain-name-brainstormer`
  - Found via `npx skills find domain naming`
  - Useful as a generic reference for brainstorming plus domain checks
  - Our local skill is intentionally narrower and more structured

Differences in this local skill:

- prioritizes brandable enterprise-AI names
- explicitly avoids weak literal roots unless requested
- uses CSV as the default working artifact
- uses public registry RDAP checks
- preserves failed and fallback candidates, not just the winners
